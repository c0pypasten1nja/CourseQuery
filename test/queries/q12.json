{
    "title": "A query with invalid order",
    "query": "In rooms dataset rooms, find entries whose Seats is greater than 80; show Seats and Address; sort in asc order by Average.",
    "response": {
        "code": 400,
        "body": {
            "error": "Invalid query string"
        }
    }
}
