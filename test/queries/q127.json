{
    "title": "QUERY   ::= DATASET + ', ' + FILTER + '; ' + DISPLAY(+ '; ' + ORDER)(+ '; ' + ORDER)? + '.'",
    "query": "In courses dataset courses, find entries whose Average is greater than 98; show Department and ID, Instructor, Title, UUID, Average, Pass, Fail and Audit; sort in ascending order by ID; sort in descending order by Average.",
    "response": {
        "code": 400,
        "body": {
            "error": "Invalid query string"
        }
    }
}
